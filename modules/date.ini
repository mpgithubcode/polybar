[module/date]
type = internal/date

interval = 60.0

date = %A, %d %B

format = <label>

label =  %date%
label-font = 1

format-foreground = ${colors.color0}
format-background = ${colors.foreground}
